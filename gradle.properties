# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class is only
# visible to the code in the same library, creating a smaller APK size and
# avoiding naming conflicts
android.nonTransitiveRClass=true

# Proxy configuration (uncomment and configure if needed)
# If you don't need a proxy, leave these commented out
# systemProp.http.proxyHost=127.0.0.1
# systemProp.http.proxyPort=8080
# systemProp.http.proxyUser=your_username
# systemProp.http.proxyPassword=your_password
# systemProp.https.proxyHost=127.0.0.1
# systemProp.https.proxyPort=8080
# systemProp.https.proxyUser=your_username
# systemProp.https.proxyPassword=your_password

# Bypass proxy for localhost (if proxy is causing issues)
systemProp.http.nonProxyHosts=localhost|127.0.0.1|*.local
systemProp.https.nonProxyHosts=localhost|127.0.0.1|*.local

# Fix for JDK/jlink issues
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -XX:+UseG1GC
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Suppress warning for compileSdk 34 with AGP 8.2.0
android.suppressUnsupportedCompileSdk=34
